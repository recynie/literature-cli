# auth.toml — secrets
# Copy to ~/.config/litcli/auth.toml (user-level) or .litcli/auth.toml (project-level).

[openai]
api_key = "sk-..."

[mineru]
# Get your free token from https://mineru.net/apiManage/token
api_key = "your_mineru_api_key_here"
